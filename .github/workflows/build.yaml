name: Build App

on:
    push:
        branches:
        - main

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v2
        - name: build-push
          uses: docker/build-push-action@v1
          with:
            username:  imlashsylmaryl
            password:  GeraltZRivii83
            registry: docker.io
            repository: imlashsylmaryl/cd
            tag_with_sha: true
